<Window x:Class="Paint.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Paint"
        mc:Ignorable="d"
        Title="Paint" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel HorizontalAlignment="Center" Grid.Row="0" Orientation="Horizontal">
            <StackPanel.Resources>
                <Style TargetType="{x:Type Button}">
                    <Setter Property="Margin" Value="20,5,0,5"/>
                </Style>
            </StackPanel.Resources>
            <Button Content="Line" Click="LineButton_Click" Width="80" />
            <Button Content="Rectangle" Click="RectangleButton_Click" Width="80" />
            <Button Content="Ellipse" Click="EllipseButton_Click" Width="80" />
        </StackPanel>
        <Canvas Grid.Row="1" Background="Transparent" Name="drawingCanvas" />

        <Canvas Grid.Row="1" Background="Transparent" Name="eventCanvas"
                MouseDown="Canvas_MouseDown"
                MouseMove="Canvas_MouseMove"
                MouseUp="Canvas_MouseUp"
                />


    </Grid>
</Window>
